@page "/schema"

<MudTabs Elevation="4" Rounded="true" Color="Color.Primary" Border="true" Outlined="true">
    <MudTabPanel Text="Schema" Icon="@Icons.Material.Filled.Schedule">
        <SchemaTab/>
    </MudTabPanel>
    <MudTabPanel Icon="@Icons.Material.Filled.Build">

    </MudTabPanel>
    <MudButton Style="position: fixed; right: 10px; bottom: 20px" Variant="@Variant.Filled" Color="Color.Success" StartIcon="@Icons.Material.Filled.Add" OnClick="OpenPage">
        Lägg till
    </MudButton>
</MudTabs>

@code {

    private void OpenPage()
    {
        NavigationManager.NavigateTo("/schema/add_event");
    }

}
